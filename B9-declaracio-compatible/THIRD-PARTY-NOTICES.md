# Third-party notices

Version 2.0.0 of this widget contains no third-party runtime code, fonts, images,
or package dependencies.

The implementation uses only browser platform APIs and Node.js built-in modules
for validation.
